export {
  signIn,
  signUp,
  signOut,
  onAuthStateChanged,
  getCurrentUser,
  db,
  getCollection,
  getDocument,
  COLLECTIONS,
  auth,
  firestore,
} from './firebase';

export { SecureStorage, Storage, STORAGE_KEYS } from './storage';
